#include<iostream>

// 빠른 입출력을 위해 cin 의 tie를 풀고 stdio와 iostream과의 싱크를 false 했다.
using namespace std;
int main() {
	cin.tie(NULL);
	ios_base::sync_with_stdio(false);

	int T, A, B;
	cin >> T;
	for (int i = 0; i < T; i++) {
		cin >> A >> B;
		cout << A + B << "\n";
	}
	return 0;
}
